var photoLinks = document.querySelectorAll('.portfolio__photo-link'); var modal = document.querySelector('.modal'); var imgContainer = document.querySelector('.modal__img'); photoLinks.forEach((link) => { link.addEventListener('click', (evt) => { evt.preventDefault(); var imgSrc = evt.currentTarget.getAttribute('href'); modal.classList.add('modal--open'); imgContainer.style.backgroundImage = `url(${imgSrc})`; window.addEventListener("keydown", (evt) => { if (evt.keyCode === 27) { modal.classList.remove('modal--open') } }) }) }); modal.addEventListener('click', () => { modal.classList.remove('modal--open') })